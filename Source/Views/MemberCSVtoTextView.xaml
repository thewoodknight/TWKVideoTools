<UserControl x:Class="TWKVideoTools.Views.MemberCSVtoTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TWKVideoTools.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnYoutube" Content="Find Members" Click="btnYoutube_Click" Padding="10,5,10,5" Margin="10" />
                <Button x:Name="btnCSV" Content="Load CSV" Command="{Binding LoadCSVCommand}" Padding="10,5,10,5" Margin="10" />
            </StackPanel>
            <Label>Output</Label>
            <TextBox x:Name="txtContent" Padding="10" Text="{Binding Content}" Height="500" />
        </StackPanel>
    </Grid>
</UserControl>
